# Dependencies
node_modules/

# Build output. Regenerated by `npm run build` and published to the
# gh-pages branch by `npm run deploy`. Never tracked on main.
dist/

# Raw working material: original photography, source design files and
# screenshots. Kept on disk so they are at hand, but deliberately never
# pushed — this repository is public and serves the live site.
project/uploads/
project/screenshots/

# OS junk
.DS_Store
Thumbs.db
desktop.ini
