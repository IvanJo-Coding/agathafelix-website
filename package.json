{
  "name": "agatha-felix-website",
  "version": "1.0.0",
  "private": true,
  "description": "Production build of the Agatha Felix marketing website (Website v2 kit).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "preview": "node build.mjs && node preview.mjs",
    "serve": "node preview.mjs",
    "test": "node --test test.mjs",
    "deploy": "node deploy.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "sharp": "^0.35.4"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
